# Скрипты для анализа BREP в Rhino

Коллекция Python скриптов для анализа и обработки геометрии Brep/Polysurface в Rhinoceros 3D.

## 📋 Обзор

Набор скриптов для:
- Определения типов поверхностей (цилиндры, конусы, плоскости, NURBS)
- Автоматического создания линий вдоль осей поверхностей
- Пакетной обработки множества поверхностей
- Геометрического анализа и статистики

## 🎯 Ключевое правило

**КРИТИЧНО: Всегда разбивайте Brep перед анализом!**

```
BREP → Explode → Отдельные поверхности → Точный анализ ✅
BREP → Прямой анализ → Ненадёжные результаты ❌
```

### Почему нужно делать Explode?

1. **Определение типов**: `TryGetCylinder()` и `TryGetCone()` работают надёжно только на отдельных поверхностях
2. **Доступ к параметрам**: UV домены и границы граней более точные
3. **Визуальный контроль**: Можно видеть и выбирать каждую поверхность отдельно
4. **Нет пропущенной геометрии**: Сложные формы правильно разделяются

## 📁 Скрипты

### 01_analyze_brep.py
Анализирует Brep и подсчитывает типы поверхностей.

**Возможности:**
- Подсчёт цилиндров, конусов, плоскостей, NURBS
- Фильтрация цилиндров по диаметру (порог >3.2мм)
- Индикатор прогресса для больших моделей
- Итоговая статистика

**Использование:**
```
1. Выделите объект Brep
2. Запустите скрипт
3. Просмотрите статистику
```

**Что делает код:**
- Получает выделенный Brep объект
- Проходит по всем граням (faces) в Brep
- Для каждой грани определяет тип поверхности:
  - Цилиндр: через `TryGetCylinder()`, проверяет диаметр
  - Конус: через `TryGetCone()`
  - Плоскость: через `IsPlanar()`
  - NURBS: проверяет класс поверхности
- Подсчитывает количество каждого типа
- Выводит итоговую статистику

**Пример вывода:**
```
📊 РЕЗУЛЬТАТЫ:
🔵 Цилиндры: 70
   └─ Ø>3.2мм: 45
🔺 Конусы: 12
⬜ Плоскости: 520
📐 NURBS: 31
```

---

### 02_check_surface.py
Детальный анализ одной выделенной поверхности.

**Возможности:**
- Определение класса поверхности
- Определение типа (цилиндр/конус/сфера/плоскость)
- Параметры (диаметр, радиус, угол, центр)
- Информация о BoundingBox
- NURBS параметры (степень, замкнутость)

**Использование:**
```
1. Выделите ОДНУ поверхность
2. Запустите скрипт
3. Просмотрите детальную информацию
```

**Что делает код:**
- Получает выделенную поверхность
- Определяет класс: BrepFace, NurbsSurface, RevSurface и т.д.
- Проверяет все типы поверхностей:
  - Цилиндр: получает радиус, диаметр, центр, ось
  - Конус: получает радиус основания, угол, вершину
  - Сфера: получает радиус
  - Плоскость: проверяет планарность
- Выводит BoundingBox (габариты по X, Y, Z)
- Для NURBS показывает степень и замкнутость по U/V
- Вычисляет центр грани через UV параметры

**Пример вывода:**
```
✅ ЦИЛИНДР
   Радиус: 2.000мм
   Диаметр: 4.000мм
   Центр: (64742, -12780, 66825)
   
BoundingBox:
  X: 64738.0 до 64746.0 (размер 8.0)
  Y: -12782.0 до -12778.0 (размер 4.0)
  Z: 66823.0 до 66829.0 (размер 6.0)
```

---

### 03_create_line_cylinder.py
Создаёт линию вдоль оси цилиндрической поверхности.

**Возможности:**
- Линия точно по оси цилиндра
- Проходит от края до края грани
- Использует проекцию UV параметров
- Зелёный цвет для линий цилиндров

**Использование:**
```
1. Выделите цилиндрическую поверхность
2. Запустите скрипт
3. Создастся зелёная линия вдоль оси
```

**Что делает код:**
- Проверяет что выделенная поверхность - цилиндр
- Получает ось цилиндра (Axis) и центр (Center)
- Находит UV параметры грани:
  - u_mid: середина в направлении U (вокруг оси)
  - v_min, v_max: границы в направлении V (вдоль оси)
- Вычисляет точки на краях грани: `face.PointAt(u_mid, v_min/max)`
- Проецирует эти точки на ось цилиндра:
  - Вектор от центра к точке: `v = point - center`
  - Проекция на ось: `t = v.X * axis.X + v.Y * axis.Y + v.Z * axis.Z`
  - Точка на оси: `center + axis * t`
- Создаёт линию между спроецированными точками
- Окрашивает линию в зелёный цвет

**Технические детали:**
```python
# Проекция точки на ось цилиндра
v1 = p1 - cyl_center
t1 = v1.X * axis.X + v1.Y * axis.Y + v1.Z * axis.Z
start = cyl_center + axis * t1
```

**Результат:**
- Зелёная линия точно по центральной оси цилиндра
- Линия в пределах грани (от края до края)
- Длина линии = высота цилиндрической грани

---

### 04_batch_process_surfaces.py
Обрабатывает множество поверхностей за один запуск.

**Возможности:**
- Автоматическое определение типа
- Создаёт линии для цилиндров (зелёные) и конусов (красные)
- Обрабатывает множественное выделение
- Показывает прогресс обработки

**Использование:**
```
1. Выделите несколько поверхностей (используйте Shift)
2. Запустите скрипт
3. Линии создадутся автоматически для всех
```

**Что делает код:**
- Получает все выделенные объекты
- Для каждой поверхности:
  1. Определяет тип (цилиндр/конус/другое)
  2. Если цилиндр:
     - Вычисляет диаметр
     - Создаёт линию вдоль оси (как в скрипте 03)
     - Окрашивает в зелёный
  3. Если конус:
     - Получает радиус и угол
     - Находит центр грани
     - Создаёт линию вдоль оси конуса (5мм длина)
     - Окрашивает в красный
  4. Если другой тип - пропускает
- Подсчитывает количество созданных линий
- Выводит итоговую статистику

**Пример работы:**
```
Выделено: 5 поверхностей

ПОВЕРХНОСТЬ #1
🔵 ЦИЛИНДР Ø4.0мм
   ✅ ЗЕЛЁНАЯ линия создана

ПОВЕРХНОСТЬ #2
🔵 ЦИЛИНДР Ø3.5мм
   ✅ ЗЕЛЁНАЯ линия создана

ПОВЕРХНОСТЬ #3
🔺 КОНУС!
   R=2.5мм Угол=45.0°
   ✅ КРАСНАЯ линия создана

✅ СОЗДАНО 3 ЛИНИЙ
```

---

## 🔧 Технические детали

### Определение типа поверхности

```python
# Цилиндр
is_cyl, cyl = surf.TryGetCylinder()
if is_cyl:
    diameter = cyl.Radius * 2      # Диаметр
    axis = cyl.Axis                # Ось (вектор направления)
    center = cyl.Center            # Центр оси

# Конус
is_cone, cone = surf.TryGetCone()
if is_cone:
    radius = cone.Radius           # Радиус основания
    angle = cone.AngleInDegrees    # Угол конуса
    apex = cone.ApexPoint          # Вершина конуса
```

### Метод проекции на ось

**Задача:** Создать линию точно по оси цилиндра, в пределах грани.

**Алгоритм:**

1. **Получить границы грани в UV пространстве:**
```python
u_mid = (face.Domain(0).Min + face.Domain(0).Max) / 2  # Середина по U
v_min = face.Domain(1).Min                              # Начало по V
v_max = face.Domain(1).Max                              # Конец по V
```

2. **Вычислить точки на краях грани:**
```python
p1 = face.PointAt(u_mid, v_min)  # Точка на нижнем краю
p2 = face.PointAt(u_mid, v_max)  # Точка на верхнем краю
```

3. **Спроецировать точки на ось цилиндра:**
```python
# Вектор от центра цилиндра к точке
v1 = p1 - cyl_center
v2 = p2 - cyl_center

# Скалярное произведение (проекция на ось)
t1 = v1.X * axis.X + v1.Y * axis.Y + v1.Z * axis.Z
t2 = v2.X * axis.X + v2.Y * axis.Y + v2.Z * axis.Z
```

4. **Вычислить точки на оси:**
```python
start = cyl_center + axis * t1
end = cyl_center + axis * t2
```

**Результат:** Линия точно по центральной оси, от края до края грани.

---

## 📊 Рабочий процесс

### Рекомендуемая последовательность

```
1. ИМПОРТ МОДЕЛИ
   ↓
2. ЕСЛИ Brep → EXPLODE (команда _Explode)
   ↓
3. ВЫДЕЛЕНИЕ ПОВЕРХНОСТЕЙ (по одной или группами)
   ↓
4. ЗАПУСК СКРИПТА АНАЛИЗА
   ↓
5. СОЗДАНИЕ ЛИНИЙ/ТОЧЕК
   ↓
6. ЭКСПОРТ РЕЗУЛЬТАТОВ
```

### Сравнение: Brep vs Отдельные поверхности

| Аспект | Brep | Отдельные поверхности |
|--------|------|----------------------|
| Определение типа | ❌ Ненадёжно | ✅ Точно |
| Скорость | ⚡ Быстро | 🐢 Медленнее |
| Контроль | ❌ Ограниченный | ✅ Полный |
| Пропуск геометрии | ⚠️ Возможен | ✅ Исключён |
| **Рекомендация** | ❌ Не использовать | ✅ Всегда использовать |

### Почему Explode критичен?

**ПРОБЛЕМА:** После Explode типы поверхностей могут измениться
- Конусы становятся RevSurface или NURBS
- `TryGetCone()` перестаёт работать на Brep

**РЕШЕНИЕ:** Анализировать отдельные поверхности
- Каждая поверхность = 1 грань
- Тип определяется корректно
- Параметры точные

**ПРИМЕР из практики:**
```
ИСХОДНЫЙ BREP (633 грани):
- Анализ: нашли 70 цилиндров
- Конусы: НЕ НАЙДЕНЫ (TryGetCone = False)

ПОСЛЕ EXPLODE (772 поверхности):
- Выделили цилиндрическую поверхность: ✅ определилась
- Выделили конусную зону: определилась как цилиндр (!)
- НО: каждая поверхность определяется точно
```

---

## ⚙️ Требования

- Rhinoceros 3D (версия 6 или новее)
- RhinoPython включен
- Python 2.7 (IronPython в Rhino)

## 🚀 Установка

### Метод 1: Через RhinoPython Editor

1. Скачайте скрипты из этого репозитория
2. В Rhino: Tools → PythonScript → Edit
3. Откройте файл скрипта
4. Запустите через F5 или кнопку Run

### Метод 2: Через MCP (Model Context Protocol)

Используйте интеграцию с Claude через MCP для автоматического выполнения скриптов.

## 📝 Примечания

- Все измерения в миллиметрах
- Tolerance: 0.01мм (стандарт Rhino)
- Цветовая кодировка:
  - 🟢 Зелёный = Линии цилиндров
  - 🔴 Красный = Линии конусов
  - 🔴 Красный = Центральные точки

## 🎓 Что я узнал

### 1. BREP нельзя анализировать напрямую
- После Explode геометрия меняется
- TryGetCone() не работает на конусах в Brep
- Нужно всегда разбивать на отдельные поверхности

### 2. UV параметры - ключ к точности
- Domain(0) = направление вокруг оси (U)
- Domain(1) = направление вдоль оси (V)
- PointAt(u, v) = точка на поверхности

### 3. Проекция на ось = скалярное произведение
- Вектор от центра к точке
- Умножаем на ось (v.X * axis.X + v.Y * axis.Y + v.Z * axis.Z)
- Получаем параметр t для точки на оси

### 4. Batch обработка экономит время
- Выделить 10-20 поверхностей
- Запустить скрипт один раз
- Все линии создаются автоматически

## 🤝 Вклад

Создано во время сессии с Claude AI для автоматизации CAD workflow.

## 📄 Лицензия

Свободно для использования и модификации в личных и коммерческих проектах.

## 📧 Контакты

Для вопросов или улучшений создайте issue на GitHub.

---

**Помните:** Всегда делайте Explode перед анализом! 🎯
